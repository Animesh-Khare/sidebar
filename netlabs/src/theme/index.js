import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#0057A2",
    },
    secondary: {
      main: "#FCFCFC",
    },
  },
});

export default theme;
